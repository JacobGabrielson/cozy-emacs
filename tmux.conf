set-option -g prefix C-z
unbind-key C-b
bind-key C-z send-prefix

# C-z C-o has been annoying lately
unbind C-o
bind C-o select-pane -t :.+
bind O rotate-window

set-option -g history-limit 50000

set-option -g status-keys emacs
setw -g mode-keys emacs

set -g base-index 1
setw -g pane-base-index 1

set-option -g set-clipboard on
# Mac only
if-shell 'test "$(uname)" = "Darwin"' \
  'set -s copy-command pbcopy'

setw -g monitor-activity on
set -g focus-events on
set -g status-position top

# # from https://github.com/JohnMurray/dotfiles/blob/master/.tmux.conf
# # or do
# #  % tmux source-file ~/.tmux.conf 
# # Note #() shell commands run every 'status-interval' seconds
bind r source-file ~/.tmux.conf \; display-message "  Config reloaded ..."

# # window layouts (emacs-like)  - dont' rly work
# #bind-key 1 break-pane
# #bind-key 2 select-layout even-vertical
# #bind-key 3 select-layout even-horizontal
# #bind-key o select-pane -U

# # resize window aggressively even if smaller client is connected to a
# # diffrent window
setw -g aggressive-resize on


set -s escape-time 10


# #bind-key  split-window -v


# set-option -g lock-command vlock


# # Since I log in from laptop a lot
set -g update-environment "DISPLAY SSH_ASKPASS SSH_AUTH_SOCK SSH_AGENT_PID SSH_CONNECTION WINDOWID XAUTHORITY"

# # automatically close gaps
# # http://unix.stackexchange.com/questions/21742/renumbering-windows-in-tmux
set-option -g renumber-windows on


# # http://superuser.com/questions/266567/tmux-how-can-i-link-a-window-as-split-window
bind-key @ command-prompt -p "create pane from:"  "join-pane -s ':%%'"

# # Scroll wheel should work
# # https://superuser.com/questions/210125/scroll-shell-output-with-mouse-in-tmux
setw -g mouse on
# # from https://unix.stackexchange.com/questions/81782/how-can-i-create-a-shortcut-to-resize-panes-in-a-tmux-window
# # disabled because interferes w/ emacs
# #bind -n M-k resize-pane -U 5
# bind -n M-j resize-pane -D 5
# #bind -n M-h resize-pane -L 5
# #bind -n M-l resize-pane -R 5

# # From https://raw.githubusercontent.com/seebi/tmux-colors-solarized/master/tmuxcolors-256.conf

# #### COLOUR (Solarized 256)

# # default statusbar colors
# set-option -g status-style fg=colour136,bg=colour235 #yellow and base02

# # default window title colors
# set-window-option -g window-status-style fg=colour244,bg=default #base0 and default
# #set-window-option -g window-status-style dim

# # active window title colors
# set-window-option -g window-status-current-style fg=colour166,bg=default #orange and default
# #set-window-option -g window-status-current-style bright

# # pane border
# set-option -g pane-border-style fg=colour235 #base02
# set-option -g pane-active-border-style fg=colour240 #base01

# # message text
# set-option -g message-style fg=colour166,bg=colour235 #orange and base02

# # pane number display
# set-option -g display-panes-active-colour colour33 #blue
# set-option -g display-panes-colour colour166 #orange

# # clock
# set-window-option -g clock-mode-colour colour64 #green

# # bell
# set-window-option -g window-status-bell-style fg=colour235,bg=colour160 #base02, red

# # https://www.hamvocke.com/blog/a-guide-to-customizing-your-tmux-conf/
# # switch panes using Alt-arrow without prefix
# bind -n M-Left select-pane -L
# bind -n M-Right select-pane -R
# bind -n M-Up select-pane -U
# bind -n M-Down select-pane -D

# # Note check out Putty config here first https://github.com/chriskempson/base16
# # BEGIN experiment from https://www.hamvocke.com/blog/a-guide-to-customizing-your-tmux-conf/
# #  modes
# setw -g clock-mode-colour colour5
# setw -g mode-style 'fg=colour1 bg=colour18 bold'

# # panes
# set -g pane-border-style 'fg=colour19 bg=colour0'
# set -g pane-active-border-style 'bg=colour0 fg=colour9'

# # statusbar
# set -g status-position bottom
# set -g status-justify left
# set -g status-style 'bg=colour18 fg=colour137 dim'
# set -g status-left ''
# set -g status-right "#[fg=yellow]#(TZ=America/Los_Angeles date '+%%Y-%%b-%%d %%H:%%M') #[fg=green] | #(TZ=UTC date -u +%%H:%%M) UTC"
# set -g status-right-length 50
# set -g status-left-length 20

# setw -g window-status-current-style 'fg=colour1 bg=colour19 bold'
# setw -g window-status-current-format ' #I#[fg=colour249]:#[fg=colour255]#W#[fg=colour249]#F '

# setw -g window-status-style 'fg=colour9 bg=colour18'
# setw -g window-status-format ' #I#[fg=colour237]:#[fg=colour250]#W#[fg=colour244]#F '

# setw -g window-status-bell-style 'fg=colour255 bg=colour1 bold'

# # messages
# set -g message-style 'fg=colour232 bg=colour16 bold'

# # git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
# set -g @plugin 'tmux-plugins/tpm'
# set -g @plugin 'tmux-plugins/tmux-sensible'

# set -g @plugin 'jatap/tmux-base16-statusline'

# # MUST be last line of the file
# run '~/.tmux/plugins/tpm/tpm'



# Thicker border lines (tmux ≥ 3.3)
set -g pane-border-lines double

# Very visible active pane border
set -g pane-active-border-style 'fg=brightyellow,bg=default'
set -g pane-border-style 'fg=colour237,bg=default'

# Subtle dimming of inactive panes (tmux ≥ 2.1)
set -g window-style 'fg=colour247,bg=colour236'
set -g window-active-style 'fg=default,bg=default'

# Make inactive windows slightly dim
set -g window-status-style "fg=default,bg=default,dim"

# Make current window very obvious
set -g window-status-current-style "fg=black,bg=yellow,bold"
set -g window-status-current-format " #I:#W#F "

# Keep default-ish format for others (index:name+flags)
set -g window-status-format " #I:#W#F "

### Truecolor / 256-color support
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",xterm-256color:RGB"  # or: set -a terminal-features 'xterm-256color:RGB'
set -g assume-paste-time 0

# Pane borders (purple accent on dark)
set -g pane-border-style        "fg=colour238"          # dull border

set -g pane-active-border-style 'fg=#ff0000,bold'
#set -g pane-active-border-style "fg=colour141"          # bright purple

# Copy / choose mode
set -g mode-style "fg=colour141,bg=colour235"           # purple on dark gray

# Messages (command prompts, warnings)
set -g message-style        "fg=colour231,bg=colour236" # white on dark gray
set -g message-command-style "fg=colour231,bg=colour236"

### Status bar
set -g status on
set -g status-bg colour234
set -g status-fg colour252
set -g status-style "fg=colour252,bg=colour234"

set -g status-left-length 40
set -g status-right-length 120

# Left: session name with purple accent
set -g status-left "#[fg=colour141,bg=colour234,bold] #S #[fg=colour234,bg=colour234]"

# Right: time and date, segmented, high contrast
set -g status-right "#[fg=colour239,bg=colour234]#[fg=colour231,bg=colour239] %H:%M #[fg=colour239,bg=colour234]#[fg=colour252,bg=colour239] %Y-%m-%d "

### Window list
# Inactive windows
setw -g window-status-style "fg=colour244,bg=colour234"
setw -g window-status-format " #[fg=colour240]#I:#W#[fg=colour244] "

# Active window: bright text on slightly lighter dark bg
setw -g window-status-current-style "fg=colour231,bg=colour236,bold"
setw -g window-status-current-format " #[fg=colour141]#I:#W#[fg=colour231] "

# Activity style
setw -g window-status-activity-style "fg=colour141,bg=colour234,underscore"

#   To change an existing layout quickly:
#   - Ctrl-z Space cycles layouts
#   - Ctrl-z Alt-1 even-horizontal
#   - Ctrl-z Alt-2 even-vertical
#   - Ctrl-z Alt-3 main-horizontal
#   - Ctrl-z Alt-4 main-vertical
# • Use resize-pane on the current pane:
#   - Taller (increase height): Ctrl-z : then resize-pane -D 5
#   - Shorter: Ctrl-z : then resize-pane -U 5
#   5 is lines; change as needed.
#   If your tmux has bind shortcuts enabled, try:
#   - Ctrl-z Ctrl-Down taller
#   - Ctrl-z Ctrl-Up shorter


